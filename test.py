# Simple end list indicator for cases when we want to make sure the FAB doesn't cover any content.
# Wraps the compat and OS versions of the Notification builders so we can more easily access native features in newer versions
# TODO investigate if this false cause any issues!
